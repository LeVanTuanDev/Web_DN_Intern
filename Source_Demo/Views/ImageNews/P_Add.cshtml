@model Source_Demo.Models.EM_ImageNews

@{
    ViewData["Title"] = "Thêm mới ImageNews";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";

    var breadCrumb = new Source_Demo.ViewModels.VM_BreadCrumb
            {
                ParentName = "Quản lý hình ảnh tin tức",
                CurrentName = "Thêm mới"
            };
}

@section Styles {
    @await Html.PartialAsync("_P_StylesTableAndFormBasic")
    <link rel="stylesheet" href="~/css/site.css" />
}
@section Scripts {
    @await Html.PartialAsync("_P_ScriptsTableAndFormBasic")
    <script src="~/js/utility_lib.js" asp-append-version="true"></script>
}

@await Html.PartialAsync("_P_BreadCrumb", breadCrumb)

<div class="card">
    <div class="card-body">
        <h4 class="card-title">@ViewData["Title"]</h4>

        <form asp-action="P_Add" method="post">
            <div class="mb-3">
                <label asp-for="news_id" class="form-label"></label>
                <input asp-for="news_id" class="form-control" />
                <span asp-validation-for="news_id" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="image_url" class="form-label"></label>
                <input asp-for="image_url" class="form-control" />
                <span asp-validation-for="image_url" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="is_avatar" class="form-label">Ảnh đại diện?</label>
                <input asp-for="is_avatar" type="checkbox" class="form-check-input" />
                <span asp-validation-for="is_avatar" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="create_at" class="form-label"></label>
                <input asp-for="create_at" type="datetime-local" class="form-control" />
                <span asp-validation-for="create_at" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-success">Lưu</button>
            <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
        </form>
    </div>
</div>
